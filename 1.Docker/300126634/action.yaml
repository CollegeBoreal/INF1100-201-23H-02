name: "Hello Franklin"
description: "Greet someone"
author: "300126634@monboreal.ca"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"

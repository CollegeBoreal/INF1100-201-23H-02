name: "Hello Actions"
description: "Greet someone"
author: "memedlebleu@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "memed"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"

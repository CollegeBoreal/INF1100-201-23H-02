name: "Hello Actions"
description: "Greet someone"
author: "zacklafhel@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "MAS"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
